{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Backend and Frontend",
            "type": "process",
            "command": "${workspaceFolder}\\start.bat",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
