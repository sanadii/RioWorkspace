import React, { useState } from "react";
import { Button, ButtonGroup, Col, Row } from "reactstrap";

const ProductComponent = ({ productRef }) => {
  return <Row className="mt-3"></Row>;
};

export { ProductComponent };
