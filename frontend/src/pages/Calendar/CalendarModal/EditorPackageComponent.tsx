import React, { useState } from "react";
import { Button, ButtonGroup, Col, Row } from "reactstrap";

const EditorPackageComponent = ({ packageRef }) => {
  return <Row className="mt-3"></Row>;
};

export { EditorPackageComponent };
